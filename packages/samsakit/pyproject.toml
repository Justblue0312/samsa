[project]
name = "samsakit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Justblue0312", email = "trao0312@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
samsakit = "samsakit:main"

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0"]
build-backend = "uv_build"
